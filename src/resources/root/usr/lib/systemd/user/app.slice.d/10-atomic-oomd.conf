# SPDX-License-Identifier: LGPL-2.1-or-later
#
# GNOME applications run under app.slice. Mark it as a kill candidate so
# systemd-oomd can terminate runaway apps before the kernel OOM killer
# takes down the whole desktop session.

[Slice]
ManagedOOMSwap=kill
ManagedOOMMemoryPressure=kill
